services:
  - type: web
    name: p2p-three-way-match-workbench
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
